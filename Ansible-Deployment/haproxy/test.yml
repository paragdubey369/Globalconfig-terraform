# # TEST: HA Test Environment
# # 2haproxy nodes 
all:
  vars:
    environment_type: "TEST"
  children:
    haproxy_nodes:
      children:
        primary_group:
          hosts:
            haproxy-primary:
              ansible_host: priv-172-17-0-18.local
              primary_ha: haproxy1
              ansible_user: rhel
              ansible_password: "{{ vault_sysuser_password }}"
        secondary_group:
          hosts:
            haproxy-secondary:
              ansible_host: priv-172-17-0-19.local
              secondary_ha: haproxy2
              ansible_user: rhel
              ansible_password: "{{ vault_sysuser_password }}"
