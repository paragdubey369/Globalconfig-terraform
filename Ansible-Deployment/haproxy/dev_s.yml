# # DEV-S: Small Development Environment
# # Single node acting as haproxy
all:
  vars:
    environment_type: "DEV-S"
  children:
    haproxy_nodes:
      children:
        primary_group:
          hosts:
            haproxy_primary:
              ansible_host: priv-172-17-0-106.local
              primary_ha: haproxy-primary
              ansible_user: rhel
              ansible_password: "{{ vault_sysuser_password }}"
