all:
  vars:
    environment_type: "DEV-M"
  children:
    haproxy_nodes:
      children:
        primary_group:
          hosts:
            haproxy-primary:
              ansible_host: 172.17.0.209
              primary_ha: haproxy1
              ansible_user: rhel
              ansible_password: "{{ vault_sysuser_password }}"
