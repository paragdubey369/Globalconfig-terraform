all:
  vars:
    environment_type: "DEV-M"
  children:
    haproxy_nodes:
      children:
        primary_group:
          hosts:
            haproxy_primary:
              ansible_host: priv-172-17-0-209.local
              primary_ha: haproxy1
              ansible_user: rhel
              ansible_password: "{{ vault_sysuser_password }}"
